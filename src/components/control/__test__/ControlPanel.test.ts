import ControlPanel from '../ControlPanel';

const fakeSlider = '.fake-slider';
const fakeControlPanel = '.fake-control-panel';
const controlPanel = new ControlPanel(fakeControlPanel, fakeSlider);

describe('Test of the "Observer" pattern, method "subscribe"', () => {
  test('test', () => {});
});
